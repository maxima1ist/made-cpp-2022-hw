#include <vector>
#include <string>

namespace hw04 {

size_t count_substring_entries(const std::vector<char> &chunk,
                               size_t chunk_size, const std::string &substr);
                               
}